<<<<<<< HEAD
<h1>Users#show</h1>
<nav class="navbar" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <a class="navbar-item" href="https://bulma.io">
      <img src="./images/logo.png" width="112" height="28">
    </a>

    <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="navbarBasicExample" class="navbar-menu">
    <div class="navbar-start">
      <a class="navbar-item">
        Home
      </a>
      </div>
    </div>

    <div class="navbar-end">
      <div class="navbar-item">

        <div class="buttons">
        <% if @user %>
           <h2 class='button is-static is-light'>Welcome  <%= @user.name %></h2>
          <%= link_to 'Log Out', logout_path, method: 'delete', class: 'button is-primary' %>
        <% else %>
        <%= link_to 'Sign Up', new_user_path, class: 'button is-primary' %>
        <%= link_to 'Log In', '/users/sign_in', class: 'button is-light' %>
        <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>
<% if logged_in? %>
<% @user.created_events.each do |event| %>
        <h2 class='subtitle'> Creator:
       <strong> <%= event.creator.name %></strong>
         </h2>  
              <%#= event.description %>
   <% end %>
<% else %>
<% @created_events.each do |event| %>
       <strong> <%= event.description %></strong>
       <small><%= event.event_date %></small>
         </h2>
         <% end %> 
<%end %>
  
=======

<section class='section container'>
<div class='content'>
<% if logged_in?  %>
  <% if current_user.created_events.any? %>
    <p> You have: <%= current_user.created_events.count %> events </p>
      <%= link_to 'Create Event', new_event_path, class: 'button is-light' %>
          <% current_user.created_events.each do |created_event|%>
            <h3 class='subtitle'><%= created_event.title %></h3>
            <p> <%= created_event.description %> <span><%= created_event.date %></span></p>
          <% end %>  
  <% else %>
  <h1>No events </h1>
   <%= link_to 'Create Event', new_event_path, class: 'button is-light' %>
  <% end %>
<% else %>
  <p> You are not logged in, you can't create event </p>
  <% Event.all.each do |created_event|%>
            <h3 class='subtitle'><%= created_event.title %></h3>
            <p> <%= created_event.description %> <span><%= created_event.date %></span></p>
          <% end %>
<% end %>
</div>
</section>

<section class='section container'>
<div class='content'>
<% if logged_in?  %>
  <% if Event.past_events.any? %>
    <p>You have the following <%= Event.past_events.count %> past events</p>
    <ul>
        <% Event.past_events.each do |event| %>
          <li><%= event.title %> | <%= event.date %> | <%= event.location %> </li>
        <% end %>  
    </ul>
  <% else %>
    <p> You have no past events. </p> 
  <% end %>

  <% if Event.upcoming_events.any? %>
    <p>You have the following <%= Event.upcoming_events.count %> upcoming events</p>
    <ul>
        <% Event.upcoming_events.each do |event| %>
          <li><%= event.title %> | <%= event.date %> | <%= event.location %> </li>
        <% end %>  
    </ul>
  <% else %>
    <p> You have no upcoming events. </p> 
  <% end %>
    
<% end %>
>>>>>>> dd4ec7d55667483125df01c863d8106849ec84e5
