<<<<<<< HEAD
<% @created_events.each do |event| %>
        <div class='subtitle'> Creator:
       <strong> <%= event.creator.name %></strong><br>
            <caption>Desc:  <%= event.description %> </caption><br>
            <small>Date: <%= event.event_date %></small>
         </div>  

           
        
    <%#= show_creator(post.user) %>
  <%end %>
=======

<section class='section container'>
<div class='content'>
<% @created_events.each do |created_event|%>
  <h3 class='subtitle'><%= created_event.title %></h3>
  <p> <%= created_event.description %> <span><%= created_event.date %></span></p>
<% end %>
</div>
</section>

<section class='section container'>
  <div class='content'>
    <h4> Upcoming Events </h4>
    <ul>
      <% if @created_events.upcoming_events.present? %>
        <% Event.upcoming_events.each do |event| %>
        <li> <strong> <%= event.title %></strong> | <%= event.date %></li>
        <% end %>
        </ul>
      <% else %>  
  <p> No upcoming Events </p>
  <% end %>
  </div>

  <div class='content'>
    <h4> Past Events </h4>
    <ul>
      <% if @created_events.past_events.present? %>
        <% Event.past_events.each do |event| %>
        <li> <strong> <%= event.title %></strong> | <%= event.date %> </li>
        <% end %>
        </ul>
      <% else %>  
  <p> No past Events </p>
  <% end %>
  </div>
</section>
>>>>>>> dd4ec7d55667483125df01c863d8106849ec84e5
